/**
 * Socket layer placeholder — Sprint 1.2 prepares structure; attach Socket.IO in Stage 5/6.
 * Emit from services via events/eventBus.js → bridge to socket gateway later.
 */
module.exports = {
  ready: false,
  note: 'Socket gateway not started in Sprint 1.2 — eventBus is the extension point.',
};
