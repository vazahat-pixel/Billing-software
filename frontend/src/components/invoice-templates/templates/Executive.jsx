import React from 'react';
import TaxInvoiceLayout from '../shared/TaxInvoiceLayout';

/** Executive — navy letterhead premium corporate tax invoice. */
export default function Executive({ data }) {
  return <TaxInvoiceLayout data={data} themeId="executive" />;
}
